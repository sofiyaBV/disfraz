services:
  cosplay.api:
    container_name: cosplay.api
    build:
      context: ./back
    ports:
      - "3000:3000"

  cosplay.db:
    image: postgres:17.3
    env_file:
      - docker.env
    ports:
      - "5432:5432"
    volumes:
      - ./data/db:/var/lib/postgresql/data
